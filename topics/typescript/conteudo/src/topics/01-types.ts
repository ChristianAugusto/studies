/* eslint-disable */

function topic() {
  /* Tipos primitivos (aqui ocorre inferência de tipos) */
  let nome: string = "Luiz";
  let idade: number = 30;
  let adulto: boolean = true;
  let simbolo: symbol = Symbol("qualquer-symbol");
  // let big: bigint = 10n;
}

export default topic;
