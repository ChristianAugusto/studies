
import React from "react";
import styled from "styled-components";

import AppStyle from "./App.style";

const AppWrapper = styled.div`${AppStyle}`;

export default props => (
  <AppWrapper>
    
  </AppWrapper>
);